name: 'Auto Assign'
on: [pull_request]
jobs:
  add-reviews:
    runs-on: ubuntu-latest
    steps:
    - uses: toshimaru/auto-author-assign@v0.0.1
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
